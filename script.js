$(document).ready(function() {
    // There are multiple - so this would return an array? console.log it
    const parallaxEls = $('div.parallax');
    console.log(parallaxEls);

    parallaxEls.parallax();





});